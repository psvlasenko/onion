Monorepository containing set of packages and usage examples
is progress
## Build

- Install dependencies
```bash
yarn i
```
- Build
```bash
yarn build
```
### Build only packages
```bash
yarn build-packages
```
